PORT=8080
JWT_SECRET=super-secret-change-me
# backend gRPC targets (the gateway translates browser REST -> gRPC)
USER_GRPC_ADDR=localhost:50051
PRODUCT_GRPC_ADDR=localhost:50052
SHOPPING_GRPC_ADDR=localhost:50053
# live stock push (gateway consumes product.stock.changed and fans out on /ws)
KAFKA_BROKERS=localhost:9092
KAFKA_CLIENT_ID=gateway
